{
    "name": "ios-news-bot",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "libxslt": "0.7"
    },
    "devDependencies": {
        "claudia": "^5.1.2",
        "ntl": "^3.0.0"
    },
    "scripts": {
        "deploy": "claudia create --version dev --region ap-southeast-2 --handler index.handler",
        "start": "ntl"
    }
}
